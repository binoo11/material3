import 'package:flutter/material.dart';
class TvShowScreen extends StatelessWidget {
  const TvShowScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tv Show'),);
  }
}
